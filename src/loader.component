<can-component tag="chat-loader">
  <style type="less">
    display: block;

    p { font-weight: bold; }
  </style>
  <template>
      {{#eq state "resolved"}}
      <content></content>
      {{else}}
      <div class="loading">Loading...</div>
      {{/eq}}
  </template>
</can-component>
